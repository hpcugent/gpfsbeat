################### Gpfsbeat Configuration Example #########################

############################# Gpfsbeat ######################################

gpfsbeat:
  # Defines how often an event is sent to the output
  period: 60s
  filesystem: ["/"]
  metrics:
      - quota
      - filesets
